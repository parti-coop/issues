.card.border-0
  .card-body.border-bottom
    .text-dark.card-title
      %span= link_to comment.user.name, comment.user
      %span.text-muted= l comment.created_at.to_date
    = simple_format comment.body, {}, { wrapper_tag: 'div' }