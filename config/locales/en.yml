en:
  activerecord:
    models:
      user: user
    errors:
      messages:
        record_invalid: 데이터 검증에 실패하였습니다. %{errors}
        restrict_dependent_destroy:
          has_one: "%{record}가 존재하기 때문에 삭제할 수 없습니다"
          has_many: "%{record}가 존재하기 때문에 삭제할 수 없습니다"
    attributes:
      user:
        email: email
        name: nickname

  site_name: Parti Issues
